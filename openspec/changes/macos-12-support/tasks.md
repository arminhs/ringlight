# Tasks

## 1. Run on macOS 12

- [x] 1.1 In `ringlight.xcodeproj/project.pbxproj`, change the project-level `MACOSX_DEPLOYMENT_TARGET` from 14.6 to 12.0 in both Debug and Release, leaving `ringlightTests` at 14.6 (design Decision 1); verify that Xcode shows macOS 12.0 under Minimum Deployments for the `ringlight` target and that ⌘B builds the app
- [x] 1.2 Fix any availability errors the build reports without raising the target, for example by selecting the macOS 10.15 `Font.system(size:weight:design:)` overload (design Decision 3); verify that `xcodebuild -project ringlight.xcodeproj -target ringlight -configuration Release build` succeeds with no availability errors or warnings
- [ ] 1.3 In `TemperatureSlider`, show `thermometer.medium` on macOS 13 and later and `thermometer` on macOS 12, using `if #available(macOS 13, *)` (design Decision 2); verify that the Temperature icon still shows in the control panel on the current macOS
- [x] 1.4 Add a Requirements section to `README.md` (macOS 12 Monterey or later, Apple silicon or Intel; building from source needs Xcode 16 or later, which runs on macOS 14.5 or later), and change the deployment target in the `openspec/config.yaml` context to 12.0 (unit tests 14.6); verify that `openspec validate --all --strict` passes and that the README renders correctly on GitHub

## 2. Build and release workflow

- [x] 2.1 Create `.github/workflows/build.yml` with the triggers (pull requests, pushes to `main`, `v[0-9]+.[0-9]+.[0-9]+` tags, `workflow_dispatch`) and a read-only `build` job on `macos-26` with Xcode 26.6 pinned through `DEVELOPER_DIR`, building the Release configuration with ad-hoc signing and the version overrides (design Decision 4); verify that a pull request run builds the app
- [ ] 2.2 Add the verification step inline in the workflow (not in a `.sh` file, because `.gitignore` ignores `*.sh`): fail unless `lipo -archs` lists `arm64` and `x86_64`, `LSMinimumSystemVersion` is `12.0`, `vtool -show-build` reports `minos 12.0` for both slices, `codesign --verify --deep --strict` passes, and, on tags, `CFBundleShortVersionString` matches the tag; verify that a normal run passes, and that the same commands fail when run on a Mac against a build made with `MACOSX_DEPLOYMENT_TARGET=13.0`
- [ ] 2.3 Zip the app with `ditto -c -k --keepParent` into `ringlight-<version>.zip`, write its `.sha256` file, and upload both with `actions/upload-artifact`; verify that a `workflow_dispatch` run produces an artifact whose app unzips and launches on the current macOS
- [ ] 2.4 Add the `release` job (tags only, `needs: build`, `ubuntu-latest`, `permissions: contents: write`) that runs `gh release create` with the zip and checksum and a one-line install note; verify with a throwaway tag such as `v0.0.1` that a release appears with both files and that the app shows version 0.0.1, check that a non-version tag and a pull request run produce no release, then delete the test release and tag
- [ ] 2.5 Add a Download section to `README.md`: a relative link to the latest release (`../../releases/latest`, so it also works in forks), unzip and move to Applications, first-launch steps for macOS 12 to 14 (Control-click › Open) and for macOS 15 and later (System Settings › Privacy & Security › Open Anyway), and a note that updates can trigger a new camera permission prompt; mention the workflow and ad-hoc-signed releases in the `openspec/config.yaml` context; verify that the link opens the latest release on GitHub

## 3. Verify on macOS 12 and current macOS

- [ ] 3.1 On a macOS 12 Mac or virtual machine, download the build from task 2.3 or 2.4 and follow the README's first-launch steps; verify that the app opens, shows its menu bar icon and draws the ring (scenario "Launch on macOS 12")
- [ ] 3.2 On macOS 12, open the control panel; verify that the Brightness, Temperature and Thickness controls each show an icon and label, that every slider, the Ring Light switch and Avoid Mouse work, and that the ring turns transparent around the pointer (scenarios "Control panel on macOS 12" and "Mouse avoidance on macOS 12")
- [ ] 3.3 On a real macOS 12 Mac, not a virtual machine: verify that the camera prompt, the mirrored preview and the camera turning off when the panel closes all work, and that the Brightness slider changes the built-in display's brightness, or silently does nothing where brightness control isn't available
- [ ] 3.4 In Activity Monitor on an Apple silicon Mac, verify that RingLight's Kind is Apple (not Intel), and, if an Intel Mac is available, verify that the downloaded build runs there (scenarios "Apple silicon Mac" and "Intel Mac")
- [ ] 3.5 On the current macOS, smoke-test the downloaded build (ring, control panel, `thermometer.medium` icon, camera preview); verify that nothing regressed compared with the pre-change app
